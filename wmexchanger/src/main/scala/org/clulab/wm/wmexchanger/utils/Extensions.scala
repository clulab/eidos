package org.clulab.wm.wmexchanger.utils

object Extensions {
  val placeholder = "."

  val lock = "lock"
  val json = "json"
  val jsonld = "jsonld"
  val yml = "yml"
  val rd = "rd"
  val gnd = "gnd"
  val jsonld_rd = s"jsonld-$rd"
  val jsonld_gnd = s"jsonld-$gnd"
}
