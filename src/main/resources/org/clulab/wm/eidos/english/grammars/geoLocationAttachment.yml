vars: org/clulab/wm/eidos/english/grammars/vars.yml

rules:

  - name: geolocation1
    priority: ${ rulepriority }
    label: ${ label }
    action: ${ action }
    pattern: |
      trigger = [mention=Location]+ # todo: do we need th + to get the mulitiple tokens???
      theme: Entity = <compound? </^nmod/ >nmod_in?
