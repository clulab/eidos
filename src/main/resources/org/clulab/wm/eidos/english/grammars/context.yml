
  - name: location-nn
    priority: ${ rulepriority }
    example: "reaching record levels in South Sudan."
    label: Location
    type: token
    pattern: |
      [norm="LOC" & !word=/KIs?/] [norm="LOC" & !word=/KIs?/]*
#      there are some cases with a LOC followed by something labeled ORGANIZATION that should be LOC

  - name: location-backoff
    priority: ${ rulepriority }
    example: "was reported through Gangura and Nabiapai"
    label: Location
    type: token
    pattern: |
      [entity="LOCATION"] [entity="LOCATION"]*

  - name: location-expand
    # find cases where a location is mislabeld in a conjunction with another location
    priority: ${ rulepriority }
    example: "was reported through Gangura and Nabiapai"
    label: Location
    type: token
    pattern: |
      (?<=[entity="LOCATION"] [word="and"]) [tag=/^NNP/ & !word=/KIs?/]+

  # - name: location-expand-with-lexicon
  #   # find cases where a location is mislabeld in a conjunction with another location
  #   priority: ${ rulepriority }
  #   example: "people returning from Ugandan refugee settlements"
  #   label: Location
  #   type: token
  #   pattern: |
  #     [chunk=B-NP] [entity=LOCATION]+


  - name: time-nn
    priority: ${ rulepriority }
    example: "reaching record levels in July."
    label: Time
    type: token
    pattern: |
      [entity="DATE"] [entity="DATE"]*
#      |
#      [entity="DURATION"] [entity="DURATION"]*

      # todo: same for time
      # make the other temporal attachment yml rules use the mention=X syntax
